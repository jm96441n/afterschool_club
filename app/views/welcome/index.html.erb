
<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Welcome!</h1>
                <% #if current_user.role == 'student'%>
                  <%= render partial: "parent_home" %>
                <% # elsif current_user.role == 'teacher' %>

                <% # elsif current_user.role == 'parent' %>
                
                <% #end %>
            </div>
        </div>
    </div>
</div>

